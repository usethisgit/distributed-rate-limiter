rate-limit:
  default-config:
    limit: 5
    window: 60

  endpoints:
    /api/login:
      limit: 3
      window: 60

    /api/payment:
      limit: 2
      window: 60

    /api/test:
      limit: 5
      window: 60

    /api/search:
      limit: 20
      window: 60